# Example environment variables
# Note: Signal-Fusion uses FREE data sources - no API keys required!

# Optional: Etherscan API key (free tier available)
# ETHERSCAN_API_KEY=your_key_here

# Optional: Custom data cache TTL (milliseconds)
# CACHE_TTL=300000

# Optional: Database connection (for production)
# DATABASE_URL=postgresql://user:pass@localhost:5432/signal_fusion
